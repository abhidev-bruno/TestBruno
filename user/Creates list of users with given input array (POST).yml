info:
  name: Creates list of users with given input array (POST)
  type: http
  seq: 7
  tags:
    - user

http:
  method: POST
  url: "{{baseUrl}}/user/createWithArray"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: default Response
    description: successful operation
    request:
      url: "{{baseUrl}}/user/createWithArray"
      method: POST
    response:
      statusText: Unknown
      body:
        type: text
        data: ""
