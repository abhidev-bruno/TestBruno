info:
  name: Create user
  type: http
  seq: 8
  tags:
    - user

http:
  method: POST
  url: "{{baseUrl}}/user"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: default Response
    description: successful operation
    request:
      url: "{{baseUrl}}/user"
      method: POST
    response:
      statusText: Unknown
      body:
        type: text
        data: ""
